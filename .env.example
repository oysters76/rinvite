# Copy to .env and fill in. Compose reads this for variable substitution.

# Required — the JWT signing key (min 32 bytes). Generate one:
#   openssl rand -hex 32
JWT_SECRET=

# Optional — Postgres password (defaults to "auth" if unset).
POSTGRES_PASSWORD=auth
